export { MessageManager, type MessageContent, type MessageOptions, type QueuedMessage, type MessageEventType, type MessageListener } from './message-manager';

// Re-export types for convenience
export type {
  WhatsAppMessage,
  MessageType
} from '../types';
